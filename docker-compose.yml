version: '3.8'

services:
  backend:
    build: .
    container_name: skincare-backend
    restart: always
    volumes:
      - .:/app
    environment:
      - TZ=Asia/Jakarta
    network_mode: "host"
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "80"]
